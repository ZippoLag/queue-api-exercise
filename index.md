# Queue API Exercise

This site is the **generated** DocFX view of the repository's documentation — a rendered copy, never a parallel hand-maintained one. The canonical sources are the Markdown files and the OpenSpec specs (`openspec/specs`), which agents and humans both read.

Start at the [README](README.md) — project identity, quickstart, and the pointer hub to every runbook.

**Source repository:** [ZippoLag/queue-api-exercise](https://github.com/ZippoLag/queue-api-exercise)

Use the **API Reference** item in the navigation bar for the generated API documentation.
